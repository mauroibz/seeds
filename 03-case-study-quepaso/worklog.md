# Work log — QuePaso

Append-only. Newest entry at the bottom. One entry per working session. Format per AGENTS.md §3.

## 2026-07-06 — Sprint 0 (in progress)
- Orientation: first unchecked sprint is Sprint 0. Repo had only docs + prompt.txt (single `initial commit`). Branch is `master` (not `main`); working on `master` as the existing default.
- Env: Node v24.16.0, npm 11.13.0, Docker 29.6.1 running. Supabase CLI not installed globally → will use as project dev dependency / `npx supabase`.
- Plan: do Sprint 0 steps 1–6 locally (scaffold, deps, local Supabase, client helpers, strings, app shell). Step 7 (hosted Supabase project + Vercel + deploy) needs owner accounts → prepare and hand off (AGENTS.md §6).
- Done (steps 1–6):
  - Scaffolded `web/` with create-next-app: **Next.js 16.2.10, React 19.2.4, Tailwind v4, App Router, TS, ESLint** (no src dir, alias `@/*`). Removed the stub `web/AGENTS.md` + `web/CLAUDE.md` create-next-app dropped (root AGENTS.md is the single source of truth).
  - shadcn/ui init (Tailwind v4 detected); base deps installed: `@supabase/supabase-js @supabase/ssr @tanstack/react-query maplibre-gl react-map-gl browser-image-compression exifr` (no peer-dep conflicts on React 19). shadcn also pulled `lucide-react`.
  - `lib/supabase/client.ts` (browser) + `server.ts` (server, async `cookies()` for Next 16). `lib/strings.ts` holds all es-AR copy. `app/providers.tsx` = TanStack Query (staleTime 30s, refetch-on-focus).
  - App shell: `components/shared/site-nav.tsx` (top bar desktop / fixed bottom bar mobile, active state from `usePathname`), `site-footer.tsx` (links to /acerca, /reglas), placeholder routes `/` (Mapa), `/novedades`, `/nuevo`, static `/acerca`, `/reglas`.
- Verified: `npm run lint` + `npm run build` pass (6 routes prerender). Dev server up; all 5 routes return 200; Spanish copy renders from strings; local Supabase REST reachable (http 200). Headless Chromium screenshots at **390px** (bottom nav shown, top bar hidden, active=Mapa) and **1280px** (top bar + footer shown, bottom nav hidden) — both correct, zero console/page errors.
- Deviations (all committed):
  1. **Branch is `master`, not `main`** (existing default). Working on `master`.
  2. **Local Supabase ports moved to `553xx`** (API 55321 / DB 55322 / Studio 55323 / SMTP 55324 / analytics 55327 / shadow 55320 / pooler 55329) in `supabase/config.toml` — default `543xx` was already bound by another local project (`usados`). Did NOT touch that project.
  3. **Supabase CLI installed as a repo-root dev dependency** (new root `package.json` + `package-lock.json`, CLI v2.109.0) instead of global — no global/sudo needed, reproducible. Invoke via `npx supabase`. Added `web/next.config.ts` `turbopack.root = __dirname` so the two lockfiles don't confuse Next's workspace-root inference. Root `.gitignore` added for root `/node_modules`.
  4. Updated `deployment-guide.md` §1 to match reality (ports, `npx supabase`, local OAuth callback port 55321).
  5. Used the local **JWT** anon key for `NEXT_PUBLIC_SUPABASE_ANON_KEY` (matches architecture.md naming; the newer `sb_publishable_…` key also works).
- Blocked / open: **Step 7 (hosted Supabase project + Vercel + deploy) is owner-blocked** — requires creating a Supabase account/project (region `sa-east-1`) and a Vercel account/project (root dir `web/`, the two `NEXT_PUBLIC_SUPABASE_*` env vars). Per AGENTS.md §6 the agent must not create accounts. The one acceptance criterion "deployed skeleton loads on a Vercel URL" is therefore unverified → **Sprint 0 tracker checkbox left UNCHECKED**. Everything the agent can prepare is done; the app builds clean and is Vercel-ready. Owner handoff steps are in `deployment-guide.md` §3–§4.
- Next: after the owner deploys (or explicitly says to skip/deploy later), flip the Sprint 0 checkbox. Otherwise Sprint 1 = PostGIS schema + read-only seeded map.
- **Update (owner, 2026-07-06): deploy deferred for later.** Sprint 0 checkbox flipped to done; the hosted deploy (step 7) is now a tracked pending owner task, not a blocker. Stopped here — did not start Sprint 1 (awaiting explicit go-ahead per AGENTS.md §2).

## 2026-07-06 — Sprint 1 (MILESTONE COMPLETE)
- Done: migrations 0001 (PostGIS + all tables/indexes) and 0002 (activity_state/hot_score fns; bump_counts/enforce_tag_rules/extend_lifecycle triggers; RLS on all base tables; pins/comments/photos_public views; pins_in_bbox/nearby_pins/feed RPCs). Seed: 30 Rosario pins (22 active / 2 fading / 6 dormant), 3 users, 11 comments, 10 likes. Map UI: MapView (react-map-gl/maplibre + OpenFreeMap liberty), clustered viewport loading, lifecycle pin styles, desktop hover popup, click→shallow `/p/{id}`→detail panel; `/p/[id]` deep-link route; lib/{pin-style,time,pins}.
- Verified (SQL, as `anon` where relevant): lifecycle distribution 22/2/6; pins_in_bbox orders non-dormant by hot_score desc then dormant last (top = pins 20, 09); pins_public + comments_public null ALL anonymous authors (0 leaks), named authors show; anon is **denied SELECT** on base tables and photo_metadata (tested with a real metadata row); feed excludes dormant on both tabs (24 rows); nearby_pins returns radius matches incl. a dormant one. lint + build pass.
- Verified (browser): **owner confirmed** the live map renders correctly — basemap, colored pins, clusters, dormant ghosts, hover popup, and the detail panel with real data.
- Deviations (committed): (a) `hot_score` is `stable` not `immutable` (reads now()) — fixed in migration + data-model. (b) Added `lng`/`lat` columns to `pins_public` because PostgREST serializes `geography` as EWKB hex; the map reads coords from these (non-identifying, anonymity intact) — migration + data-model updated. (c) Write-path guards (handle_new_user, rate limits, reject_banned, auto_hide) and all write/admin RPCs intentionally deferred to their sprints (2/3/5). (d) `feed` "populares" uses a created_at cursor as a Sprint-1 approximation; Sprint 4 refines pagination. (e) DB-level constraint messages (e.g. tag limit) are Spanish in SQL — frontend will map to friendly copy later; not `strings.ts`.
- Headless-test note: the map renders blank under **headless** software-WebGL (SwiftShader) because the GL context is lost before load — an environment artifact, not a product bug (owner verified it works in a real browser). Playwright map smoke tests (Sprint 5) will need a GPU or the WebGL context-loss worked around.
- Env note: local Supabase must be running (`npx supabase start`) and `web/` dev server (`npm run dev`) for the map to load pins.
- Blocked/open: none. Next: Sprint 2 — Google auth + pin composer (login, `handle_new_user`, two-step composer, photo pipeline with EXIF strip, `create_pin`).

## 2026-07-07 — Sprint 2 (increments 1–3)
Goal: log in with Google + publish a pin with photos. Working in increments.
- Done + verified:
  - **Migration 0004** (auth_write_rpcs_storage): `handle_new_user` (auto profile, unique username), `reject_banned` + `enforce_rate_limits` triggers (10/100/300/20 per 24h, Spanish errors), `create_pin` + `register_photo` security-definer RPCs, `photos` storage bucket (public read / authenticated insert / webp+jpeg / 5MB), authenticated base grants incl. **column-limited** profiles UPDATE (blocks self-promotion to admin — verified).
  - **Auth UI**: `proxy.ts` (Next 16 rename of middleware) session refresh, `/auth/callback` code exchange, `AuthProvider` context, `LoginDialog` (Google + dev-only local email), nav auth control + avatar menu, "Nuevo" gated → login dialog. Verified headless: gate/login/nav/signout.
  - **Composer** `/nuevo`: 2-step wizard (LocationPicker crosshair map + Photon search + Nominatim reverse → details form with TagInput/PinStylePicker/PhotoUploader/AnonToggle). `lib/geo`, `lib/images`. Submit = create_pin → upload → register_photo → /p/{id}.
  - **Acceptance checks passed** (real-browser Playwright publish, mobile 420px): end-to-end publish works; uploaded webp has **zero EXIF** while `photo_metadata` retains original EXIF+GPS+hash (admin-only) — P4 hybrid verified; **anonymous pin shows "Anónimo" in a second session**; rate-limit trigger rejects the 11th pin with the Spanish error (verified via API).
- Deviations / notes:
  - **Local login is dev email** (Google reserved for hosted). Login dialog shows a dev-only email form (NODE_ENV≠production). Owner: to test locally you can create an account with any email+password in that form. Google needs owner's Cloud credentials (see below).
  - **Local infra quirk**: after `supabase db reset`, Kong can hold a stale upstream → 502 on /auth. Fix: `docker restart supabase_kong_quepaso supabase_rest_quepaso` (or `supabase stop && start`).
  - shadcn components are **base-ui** based (not radix): use base-ui APIs (Trigger is a button; MenuLabel must be inside DropdownMenuGroup; no `asChild`).
  - Migration numbering: files are named `..._schema`, `..._functions...`, `..._auth_write_rpcs_storage` (3 migrations total so far; "0004" label above is informal — it's the 3rd file).

## 2026-07-08 — Sprint 2 (step 6 → MILESTONE COMPLETE)
Goal: author affordances — edit / delete / own-anonymous badge. Completes Sprint 2.
- Done:
  - **Migration `20260708120000_author_edit_delete_rpcs`**: `update_pin(p_pin_id, p_title, p_description, p_tags)` — security-definer, author-only, **24 h edit window** (from `created_at`), stamps `edited_at`; tags re-normalized by the existing `enforce_tag_rules` BEFORE-UPDATE trigger. `delete_pin(p_pin_id)` — author-only **soft delete** (`status='deleted'`), no time limit. Both `grant execute` to authenticated. data-model §4 updated with both signatures.
  - **`lib/pins.ts`**: `fetchOwnPin` (reads RLS-gated base `pins` row → identifies ownership, and works for the user's OWN anonymous pins whose `author_id` is nulled in `pins_public`), `withinEditWindow`, `updatePin`, `deletePin`.
  - **`PinPanel`**: owner-only affordances — inline edit form (title/description/`TagInput`) shown when within 24 h else a "ya pasaron 24 h" note; delete with an in-panel confirm step; **"Publicado como anónimo"** badge on own anonymous pins; **"(editado)"** marker when `edited_at` set. `MapView` passes `onMutated={fetchViewport}` so edit/delete refresh the map. New es-AR strings under `strings.pin`.
- Verified:
  - **DB layer (SQL, as `authenticated` with JWT claims):** happy-path edit stamps `edited_at` and normalizes tags (`['  Hola ','#Mundo','MUNDO','Hola','café'] → {café,hola,mundo}`); non-owner edit **rejected**; edit past 24 h **rejected**; owner soft delete sets `status='deleted'` and the pin **disappears from `pins_public`**. `pins_public` correctly nulls `author_id` on anonymous pins (confirmed via REST).
  - lint + build clean.
- **Owner-verified (real browser, 2026-07-08): edit works.** The panel affordance UI couldn't be smoke-tested headlessly (`PinPanel` renders inside `MapView`, and MapLibre's WebGL context dies under headless SwiftShader — same artifact as Sprint 1's blank map, works on a real GPU), so the owner confirmed it live. Editar → save confirmed working. Delete + own-anon badge share the same verified RPC/render path.
- **Local test fixtures (runtime only, NOT in seed.sql — gone on next `db reset`):** set a bcrypt password on seed user `valentina@example.com` = `probar123` (dev email login), and created two fresh owned pins `20000000-…-a1` (normal) / `…-a2` (anonymous) inside the 24h edit window so edit/delete/anon-badge are testable without the composer.
- Formal Sprint 2 acceptance criteria (publish <2 min, zero-EXIF upload, `photo_metadata` retains EXIF, anon shows "Anónimo" in a 2nd session, rate-limit rejects 11th pin) were all verified in the increments-1–3 entry above. **Sprint 2 checkbox flipped.**

## 2026-07-08 — Sprint 3 (MILESTONE COMPLETE, pending owner UI pass)
Goal: the detail panel — full history view + pile-on actions (comments, photos, likes, lifecycle).
- Done:
  - **Migration `20260708130000_comment_like_rpcs`**: `create_comment(pin,body,anon)` (security-definer; reuses reject_banned + enforce_rate_limits(100/day) + extend_lifecycle + bump_counts triggers) and `toggle_like(pin,anon)` (insert→liked / exists→delete→unliked; returns resulting state). Grants to authenticated. Report RPC stays deferred to Sprint 5 (moderation) — UI shows a "próximamente" placeholder per guide.
  - **Data helpers**: `lib/comments.ts` (fetchComments, fetchOwnCommentIds via RLS base-table read for "vos (anónimo)", createComment), `lib/photos.ts` (fetchPhotos, photoUrl, shared `uploadProcessedPhotos` used by BOTH composers — the pin composer was refactored onto it), `lib/likes.ts` (fetchMyLike, toggleLike), `lib/pins.ts` fetchNearbyPins.
  - **`PinPanel` rework** (product §6/§6.1/§7): header with like (optimistic), share=copy-link, report placeholder, state badge; **fading/dormant lifecycle banners**; description + tags; **`PhotoGallery`** (chronological, attributed, interleaves pin + comment photos) with a keyboard-navigable **lightbox**; **`CommentList`** (flat, "Anónimo"/"vos (anónimo)", inline comment photos open the shared lightbox) + **`CommentComposer`** (text ≤2000, ≤4 photos via `PhotoUploader max=4`, AnonToggle); **`NearbyList`** (nearby_pins; click swaps the panel via new `onOpenPin`). `PhotoUploader` gained a `max` prop. Author edit/delete affordances retained.
  - **SSR OpenGraph** on `/p/[id]` via `generateMetadata`: title (bare, layout template adds "· QuePaso"), description excerpt, `og:image` = first photo's public URL (twitter card upgrades to summary_large_image when a photo exists).
- Verified (SQL as `authenticated` with JWT claims, cross-txn where `now()` matters):
  - **Lifecycle:** non-author comment revives a **dormant** pin (ghost → active, +14 d); non-author **like** extends **~48 h**; **author self-comment does NOT move `expires_at`** — confirmed on a normal pin AND on an **anonymous** pin (uses internal author_id).
  - **Comment w/ photo** (register_photo w/ comment_id) bumps `photo_count`; **toggle_like** round-trips like_count 0→1→0; **hot-score reprioritization**: giving a sibling pin 5 likes flips its order above a 0-like pin in `pins_in_bbox` (map refetches via `onMutated`).
  - **Anonymity:** `comments_public` nulls author on anonymous comments (author still sees "vos (anónimo)" via the RLS base-table id set).
  - **OG (SSR, headless-safe):** curl of `/p/{id}` emits correct `og:title/description/image` + twitter tags; title no longer doubled.
  - lint + build clean.
- **Pending owner UI pass (real browser, same WebGL caveat as Sprints 1–2):** the panel is a child of the map, so the interactive surface (optimistic like button, comment box with photo, gallery lightbox, "otros pines cerca" swap, lifecycle banners) needs a quick owner smoke — headless SwiftShader can't render it. All backend behavior + SSR OG are fully verified. Repro on `valentina@example.com` / `probar123`: open a pin, like it, comment with a photo, tap a photo, click a nearby pin.

## 2026-07-09 — Sprint 4 (MILESTONE COMPLETE, pending owner UI pass)
Goal: the doomscroll surface + identity pages — feed (Nuevos/Populares), tag search, own profile, public profiles.
- Done:
  - **Migration `20260709120000_profiles_public_and_rpcs`**: `profiles_public` view (only `id, username, display_name, avatar_url, created_at` — never `role`/`is_banned`); `profile_pins(username,cursor,limit)` returns `setof pins_public` filtered to the user's **named** pins (anonymous pins have null `author_id` in `pins_public` → auto-excluded), **includes dormant**, keyset by `created_at desc`; `profile_comments(...)` gates `is_anonymous=false and status='published'`; `my_comments(cursor,limit)` = caller's own comments **including anonymous** (badged), `where author_id = auth.uid()`. Grants: profile_* to anon+authenticated, my_comments to authenticated.
  - **Migration `20260709130000_feed_tag_includes_dormant`**: fixed the `feed` RPC so the dormant exclusion is gated on `p_tag is null` — dormant pins are hidden from Nuevos/Populares **browsing** but surface in **tag search** (acceptance requirement).
  - **`lib/feed.ts`**: `fetchFeed({city,tab,tag,cursor,limit})` via `feed` RPC; `fetchCoverThumbs(pinIds)` (one `photos_public` query, oldest non-comment photo per pin → thumb Map).
  - **`lib/profiles.ts`**: types + `fetchPublicProfile`, `fetchProfilePins`, `fetchProfileComments`, `fetchMyComments`, `fetchMyPins` (base `pins` RLS read incl. anonymous, `activity_state` computed client-side), `updateUsername` (maps 23505→taken / 23514→format). `lib/pins.ts` gained `activityState()` (client mirror of the DB function).
  - **`/novedades`** (`FeedScreen`): Nuevos/Populares tabs, `useInfiniteQuery` keyset pagination + IntersectionObserver infinite scroll, tag search box (`?tag=` deep-link; hashtags in `PinPanel` now `Link` here), empty states. **`PinCard`** (shared feed/profile card per product §8: icon+color, cover thumb, byline w/ anon `EyeOff`, state badge, like/comment counts).
  - **`/perfil`** (auth-gated): username editor (uniqueness/format error handling → `refreshProfile`), **Mis pines** (`fetchMyPins`, includes own **anonymous** badged), **Mis comentarios** (`fetchMyComments`, anon badged). Avatar menu gained "Perfil" + "Ver perfil público" links (base-ui `render` prop, not `asChild`).
  - **`/u/{username}`**: public profile — Pines/Comentarios tabs, **non-anonymous content only** (all data from `profiles_public` + `profile_pins`/`profile_comments`), `notFound` state. `generateMetadata` sets `@username` title.
- Verified (SQL; public surfaces as `anon` role, owner surfaces via JWT claims):
  - **Feed / dormant**: dormant pin absent from `feed('Rosario','nuevos')` and `('populares')`; **present** in `feed(...,'riesgo')` tag search (0/24 and 0/… dormant browsing, 1/4 in tag search).
  - **Anonymity boundary (blocking review) — PASSED**: for `vale_rosario` (4 anon + 8 named pins, 1 anon + 3 named comments): `profile_pins` returns exactly **8**, **0 anonymous leaked**, 1 dormant shown; `profile_comments` returns exactly **3** (= named-published count), the anonymous comment never appears; owner's `my_comments` returns **4** with the 1 anonymous **shown** (badged). `profiles_public` exposes only `id, username, display_name, avatar_url, created_at` — no `role`/`is_banned`.
  - lint + build clean (routes `/novedades`, `/perfil`, `/u/[username]` all present).
- **Pending owner UI pass (real browser):** feed infinite scroll + pull-to-refresh feel, tag tap → filtered feed, username edit round-trip, and the profile cards need a quick owner smoke on a real device/GPU (feed/profile pages themselves don't embed the map, but a live pass confirms the doomscroll + edit UX). All backend behavior + the anonymity boundary are fully SQL-verified. Repro: `/novedades` (scroll, switch tabs, search a tag), `/perfil` (rename, check Mis pines/comentarios), open your `/u/{username}` and confirm no anonymous content shows.

## 2026-07-09 — UX: viewport feed rail (owner request, out-of-sprint)
Goal: surface a live list of the pins visible on the map, next to the map — three zones: viewport feed (left) · map (center) · pin detail (right). Owner-driven, planned + approved before coding.
- Decision **P10** (decisions-log): overlay rails over docked columns. Desktop = feed/detail float over the full-bleed map's edges (map never resizes → simplest); mobile = map on top (~45vh, collapsible) + feed below, detail stays a bottom sheet.
- Done:
  - **`components/feed/viewport-feed.tsx`** (new): "En esta zona" list. Reuses MapView's existing `pins_in_bbox` result (hot-ordered, **no extra query**) and the shared **`PinCard`**. Header + count, empty/loading/error states. No cover thumbnails (a viewport can hold ~200 pins and refetches on every pan — deferred as a throttled enhancement).
  - **`components/pin/pin-card.tsx`**: optional `onSelect?`/`selected?` props — card stays an `<a href="/p/{id}">` (middle-click / new-tab preserved) but left-click opens the pin in place and highlights the card. Backward-compatible: `/novedades`, `/perfil`, `/u/{username}` pass neither → unchanged.
  - **`components/map/map-view.tsx`**: keeps the full `PinPublic[]` in state and feeds `ViewportFeed`; `openPin(id, coords?)` now flies the map to a feed-selected pin; mobile map-collapse toggle calls `map.resize()` on toggle (MapLibre's `trackResize` only watches the window). Responsive shell: root `absolute inset-0 flex flex-col sm:block` — map region full-bleed on desktop / `h-[45vh]` block on mobile, feed as a `sm:fixed` left rail / in-flow block below.
  - New es-AR strings under `strings.map` (feedTitle/feedCount/feedEmpty/collapse/expand).
- Bug caught in owner's first look (**both map + list blank**): root had been `flex h-full`, but `height:100%` can't resolve through the layout's `min-h-full` flex chain (min-height ≠ definite height), collapsing everything to 0. Fixed by returning the root to `absolute inset-0` (fills the parent like the original map did) with the flex column layered inside. Owner then confirmed "perfect".
- Verified: lint + build clean; owner real-browser confirmation (map/feed is WebGL — not headless-testable). Regression: existing card surfaces still navigate.
- Deferred (noted in spec §4.3): rail cover thumbnails (throttled), a Nuevos/Populares sort toggle in the rail, hover-card→highlight-map-pin.

## 2026-07-09 — Sprint 5 (MILESTONE COMPLETE)
Goal: moderation baseline + hardening — reports, auto-hide, admin queue, legal/privacy, Sentry, Playwright smoke.
- Done (DB — migration `20260709140000_moderation_and_admin.sql`):
  - `create_report(target_type,target_id,reason,details)` (security-definer; reuses reject_banned + enforce_rate_limits(20/day); unique(target,reporter)→friendly "Ya denunciaste"). `auto_hide_on_reports` AFTER-insert trigger: 3 open reports = 3 distinct reporters (guaranteed by the unique constraint) → target `status='hidden'`.
  - Admin RPCs, each asserting `is_admin()` and logging `moderation_actions`: `admin_set_status` (hide/unhide/delete; hiding/deleting marks open reports `actioned`), `admin_dismiss_reports` (reports→`dismissed` + restores an auto-hidden target to `published`), `admin_ban_user(user_id, ban bool default true, note)` (ban/unban; content left as-is). Admin reads: `admin_report_queue()` (open reports grouped by target, with the REAL author of anonymous content + the reporters — the one sanctioned reveal, product §10) and `admin_moderation_log(limit)`. Both gated by `where is_admin()` → 0 rows for non-admins. data-model §4 updated to match.
  - **Seed:** added a 4th user `moderador@example.com` (`role='admin'`); all four seed users now have dev password `probar123`. Also set the gotrue token columns (`confirmation_token`, `recovery_token`, `email_change*`, `phone_change*`, `reauthentication_token`) to `''` — NULLs there make gotrue password login 500 with "Database error querying schema". This is why dev email login now works deterministically from seed (previously a runtime fixture).
- Done (UI):
  - `ReportButton` (shared) + `create_report` wiring (`lib/reports.ts`): reason picker (spam/ofensivo/info_personal/lugar_incorrecto/otro) + optional details, "gracias" / "ya lo denunciaste" states. Wired into the pin panel header, each non-own comment, and the photo lightbox. Removed the Sprint-3 "próximamente" report placeholder.
  - **"En revisión"** author notice: `fetchOwnPin` now reads `status`; when a pin is auto-hidden the public view returns null but the author still sees it (title+description) with the amber "en revisión" banner in the panel.
  - `/admin` (`AdminScreen` + `lib/admin.ts`): Denuncias queue (grouped by target, content preview incl. photo thumb, real-author reveal, per-report reasons) with Descartar/Ocultar↔Restaurar/Eliminar/Banear actions; Registro (moderation log) tab. Role-gated three ways: edge middleware redirect (non-admin→/), server-component role check, and the is_admin()-gated RPCs.
  - Legal/footer: `/reglas` expanded to moderation-policy §2 (6 content rules) + enforcement; new `/privacidad` page discloses the anonymity guarantee AND the EXIF-retention policy in plain es-AR; footer gained Privacidad + Contacto (mailto). Contact email is a placeholder `hola@quepaso.com.ar` (owner: swap once the inbox exists).
  - **Sentry** (`@sentry/nextjs`) wired via `instrumentation.ts` (+ `onRequestError`) and `instrumentation-client.ts`, both no-ops unless `NEXT_PUBLIC_SENTRY_DSN` is set (owner task for hosted). `.env.example` documents the var.
- Verified (SQL as anon/authenticated/admin via JWT claims): create_report happy path; duplicate→friendly error; **anonymity boundary PASSED** — `admin_report_queue()` reveals `author_username` of an anonymous pin to an admin while `pins_public` still nulls it, and a non-admin gets **0 rows** from the queue AND `permission denied` on `photo_metadata`; auto-hide flips published→hidden at exactly the 3rd distinct reporter and the pin leaves `pins_public`; `admin_dismiss_reports` restores it + closes the 3 reports + logs; `admin_set_status hide` actions reports + logs; `admin_ban_user` sets `is_banned` and the banned user's next write is rejected; non-admin admin RPC → "No autorizado". Rate-limit caps fire with exact Spanish messages: reports **20**, comments **100** (pins **10** verified Sprint 2); all four `rate_limit_*` triggers present (likes **300** shares the identical branch — unreachable with 30 seed pins).
- Verified (Playwright, **headless, real browser** — `web/e2e/smoke.spec.ts`): full moderation loop passes in ~20s — signup → publish pin **with photo** → comment **anonymously** ("vos (anónimo)") → like → report → sign out → admin login → **admin dismisses** in `/admin` → moderation log shows it; plus a second test: non-admin hitting `/admin` is redirected home. Stable across repeated runs.
- **WebGL unblock (important for future sprints):** the long-standing "MapLibre blank under headless" blocker is SOLVED by launching Chromium with `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader --ignore-gpu-blocklist` (in `playwright.config.ts`). The map, composer, and panel all render and are fully testable headlessly now. Run: `cd web && npm run e2e` (needs local Supabase up + a fresh-ish seed; `supabase db reset` first if rate limits from prior runs bite).
- Deviations/notes: (a) `admin_ban_user` gained a `ban boolean` param (covers unban) vs data-model's original 2-arg sketch — doc updated. (b) The 3-account auto-hide→dismiss→visible cycle is verified via SQL (not a 3-browser UI test); the single-report→dismiss path is the UI smoke. (c) The smoke test reports the author's own pin (1 report, no auto-hide) — enough to exercise create_report + the admin dismiss UI. (d) Orphan-photo cleanup (storage objects whose `photos` row was hard-deleted — currently only soft-deletes happen) is a documented manual chore for the Sprint-6 runbook; no orphans are produced by the MVP write paths.
- Blocked/open: none. Next: Sprint 6 — production launch (owner-gated: domain, Google OAuth, hosted Supabase/Vercel, Sentry DSN — see `deployment-guide.md`).

## 2026-07-19 — Sprint B: Branding & visual identity (MILESTONE COMPLETE)
Goal: turn `mood.png` into a shipped brand before the production launch. Extra sprint (owner request), planned + approved (plan file), decisions-log **P11**. Owner picks: light-default + dark toggle (not the moodboard's dark-default note); logo generated self-serve from the Pacifico script font (not a designer); map follows the theme.
- **Tokens** (`app/globals.css`): replaced the stock shadcn neutral palette with the brand blues in `:root` (light default) + `.dark` (toggle). Everything flows through the shadcn semantic vars, so the whole app recolored from this one file. Pin category palette (`lib/pin-style.ts`) left untouched (separate system). Added an `h1–h4` type scale.
- **Type** (`app/layout.tsx`): Geist → **Poppins** (heading) + **Inter** (body) via `next/font`. Fixed the now-dangling `--font-geist-mono` map var → system mono stack.
- **Theme** (`next-themes`): `ThemeProvider` (light default, class strategy) in `providers.tsx`; `suppressHydrationWarning` on `<html>`; sun/moon `ThemeToggle` in the nav; es-AR strings.
- **Logo**: `scripts/gen-brand-assets.mjs` renders "quepaso"/"q" from the Pacifico OFL font (via `opentype.js`) to outline SVGs (`public/brand/wordmark.svg`, `mark-q.svg`, `app-icon.svg`) — no runtime webfont dep — then `sharp` rasterizes the app-icon tile into `app/icon.png`, `apple-icon.png`, a multi-size `favicon.ico`, and `public/brand/icon-192/512.png`. `<Logo>` (`components/brand/logo.tsx`) is a CSS mask filled with `currentColor` → flips with the theme; wired into nav + footer.
- **PWA + share**: `app/manifest.ts` (theme_color `#1E40AF`, maskable icons); `metadataBase` + default OG/Twitter + `themeColor` viewport in `layout.tsx`; branded `next/og` cards — `app/opengraph-image.tsx` + `twitter-image.tsx` (site) and `app/p/[id]/og/route.tsx` (per-pin fallback), sharing `lib/og/brand-card.tsx`. Pin OG stays **photo-first**; the branded card is only the no-photo fallback (proven photo path untouched).
- **Map** (`components/map/map-view.tsx`): `scripts/gen-dark-map-style.mjs` forks OpenFreeMap positron → `public/map/dark.json` (same free tiles/glyphs/sprite, recolored). `MAP_STYLE_LIGHT`/`_DARK` in `lib/pin-style.ts`; `useTheme()` picks the style, react-map-gl swaps it live. Dormant-pin panel swatch switched from hardcoded greys to `var(--muted)`/`--muted-foreground` so it themes.
- **Verified**: `npm run lint` + `npm run build` clean. **Playwright smoke green** (2/2 — full moderation loop publish→comment→like→report→admin dismiss still passes under the new brand; no regression). Manual Playwright screenshots at 390px + desktop in **both themes**: nav/logo/toggle, map (light liberty ↔ dark style both render), viewport feed, `/novedades` cards, footer, mobile bottom nav — all coherent and on-brand. Branded OG card renders (`/opengraph-image`). App-icon `q°` tile matches the moodboard variation.
- **Anonymity boundary: N/A** — no queries/views/RPCs touched; purely presentational + metadata. Stated explicitly per AGENTS.md.
- Deviations/notes: (a) logo is a Pacifico approximation, **not pixel-identical** to `mood.png` (owner-approved); `<Logo>`/`public/brand/*` is a drop-in swap if a hand-drawn SVG arrives later. (b) `opentype.js`/`sharp` moved to devDependencies (build-time only). (c) `assets/fonts/*.ttf` are committed and read by the OG routes at runtime via `process.cwd()` — confirm they're bundled on the first Vercel deploy. (d) theme-toggle's mount guard needed a scoped `react-hooks/set-state-in-effect` eslint-disable (standard next-themes hydration pattern).
- Blocked/open: none. Next: Sprint 6 — production launch.

## 2026-07-19 — Sprint C: UX/UI polish (MILESTONE COMPLETE)
Goal: fix five owner-flagged rough edges on the map/feed/panel + three approved extras, before launch. Planned + approved (plan file), decisions-log **P12**. Owner picks: locate-with-Rosario-fallback; `⋮` overflow menu; all three extras.
- **#5 Topbar** (`site-nav.tsx`): dropped the centered `max-w-5xl` band → full-bleed `justify-between`, logo hard-left / theme+auth hard-right.
- **#1 Locate-on-load** (`map-view.tsx`): on init, geolocate and `flyTo` the user only if within `ROSARIO_MAX_KM` (50 km, `lib/pin-style.ts` + `distanceKm` haversine); else stay on Rosario. Silent on denial.
- **#2 Feed empty until zoom — ROOT CAUSE FOUND.** react-map-gl's `onLoad`/`onIdle` never fired the first `pins_in_bbox` fetch (they fire before the React handlers attach and don't replay), so the feed only filled on the first `onMoveEnd` (the user's pan/zoom). **Not headless-specific — the real bug.** Fix: an imperative effect polls for the map ref via rAF and fetches immediately using `map.getBounds()` (valid from `initialViewState` — no need to wait for tiles/style). Verified: cold load now fires the RPC and shows "27 pines" with no interaction.
- **#3 Opened pin missing from map** (`map-view.tsx` + `pin-panel.tsx`): the open pin always renders a highlighted teardrop `<Marker>` (HTML, above the GL cluster layer) with a ping ring; coords/color come from `openPin(coords)` and authoritatively from a new `PinPanel.onLoaded(lng,lat,color)` callback, which also `flyTo`s deep-linked pins that are off-screen.
- **#4 Panel declutter** (`pin-panel.tsx`): removed the redundant "QuePaso" label and the duplicate comment/photo counter row; reordered to title → byline → **description** → tags → photos → lifecycle banner → one slim engagement bar (♥ · 💬 · Compartir) → comments (count in heading) → nearby. Report + owner Edit/Delete moved into a `⋮` header menu (Base UI `DropdownMenu`). **Also fixed a latent bug**: the desktop panel was `sm:inset-y-0` (top:0) so its header sat *behind* the sticky topbar — now `sm:top-14`. Refactored `report-button.tsx` to expose a controlled `ReportDialog` (reused by the menu; `ReportButton` is now a thin wrapper).
- **Extra A — teardrop pins**: `gen-brand-assets.mjs` emits `public/map/pin-{color}.png` (8 colors + dormant) reusing the pin path; unclustered layer is now a **symbol** layer (`icon-image` by color), images registered via `addImage` on init + re-added on `styledata` after theme swaps; `color` added to `PinFeatureProps`. Clusters + fading halo unchanged.
- **Extra B — skeletons + empty CTA**: `components/ui/skeleton.tsx`; feed + panel show skeletons while loading; empty viewport shows a "Sé el primero — publicá un pin" CTA (`requireAuth → /nuevo`).
- **Extra C — feed↔map hover sync**: `hoveredId` lifted into `map-view`; hovering a `PinCard` draws a soft ring `<Marker>` on the matching pin and the map's `onMouseMove` highlights the card. Also themed the last hardcoded dormant greys in `pin-card.tsx` (`var(--muted)`).
- **Verified**: lint + build clean; **Playwright smoke 2/2** (updated two selectors: report is now via the `⋮` menu; the published title now also appears in the feed so the heading assertion is panel-scoped — the latter change is itself proof #2 works). Manual screenshots both themes at 390px + desktop: cold-load feed populated, teardrop pins render (light + dark), selected pin anchored, decluttered panel below the nav, topbar edge-aligned.
- **Anonymity boundary: N/A** — no queries/views/RPCs changed (presentation/interaction only).
- Deviations/notes: (a) the `onLoad`/`onIdle` unreliability is the key learning (P12) — future map work should not depend on those events for first paint. (b) Base UI `DropdownMenu` uses `render`/`onClick`, not Radix's `asChild`/`onSelect`.
- Blocked/open: none. Next: Sprint 6 — production launch.

## 2026-07-21 — Sprint D: Social UX/UI pass (MILESTONE COMPLETE)
Goal: make it feel like a fun, social place, not a data-analyst dashboard. Extra sprint (owner request), planned + approved (plan file), decisions-log **P13**. Owner picks: energetic + social but **no emoji/confetti**; the color+icon "chapita" stays a prominent hero; Waze-style compose "+"; fold Novedades into the map rail. **Presentation-layer only — no migration/view/RPC/query touched.**
- **W0 Foundations** (`app/globals.css`): added a warm accent token (`--accent-warm` + foreground) to `:root`/`.dark` for the FAB/active-tab/highlights (blue stays the identity); reusable motion utilities (`animate-sheet-in`, `animate-heart-pop`, `animate-fab-breathe`, `.shimmer`, `.lift`), **all disabled under `prefers-reduced-motion`**.
- **W1 Rail = one browsing surface** (owner #2): reworked `viewport-feed.tsx` into a **Cerca · Nuevos · Populares** segmented control + tag search. Cerca = the existing `pins_in_bbox` viewport list (no extra fetch); Nuevos/Populares = `feed` RPC via `useInfiniteQuery` + `fetchCoverThumbs`, infinite scroll (root = rail). Clicking any card opens the panel in place and flies the map (feed rows carry lng/lat). `/novedades` now renders the map home with the rail in feed mode (`?tag=` deep-links); removed the standalone `feed-screen.tsx`. `MapView` gained `initialRailMode`/`initialTag` + an `onCompose` handler.
- **W2 Panel declutter** (owner #1): `pin-panel.tsx` now leads with a **hero cover photo**, condensed header, and a **sticky footer comment composer** (`comment-composer.tsx` gained a `compact` mode: one-line chat bar that expands on focus). "Otros pines cerca" collapses (`nearby-list.tsx`, default closed, themed its greys). Like button heart-pops on like. Panel springs in.
- **W3 Composer** (owner #3): new **Waze-style warm "+" FAB** (`compose-fab.tsx`) on the map's lower-left (clears the desktop rail; above the mobile nav). `/nuevo` collapsed from a 2-step wizard to **one sheet**: photo → title → description → **chapita hero** (`pin-style-picker.tsx` now shows a live teardrop preview with the glyph inside; `ChapitaPreview` exported) → compact inline location map → "Más opciones" (address/tags) → anon → Publicar. **Keyword smart-suggest** (`suggestStyle` in `pin-style.ts`) pre-selects a color+icon from the text — **only ever valid `PIN_COLOR_KEYS`/`PIN_ICON_KEYS`**, so `create_pin` constraints always hold. Write path (`create_pin` + `uploadProcessedPhotos` + EXIF strip) unchanged.
- **W4 Expressive pins/cards**: `pin-card.tsx` gained `.lift` hover; map unclustered teardrops bumped larger (icon-size 0.72→1.05). Deferred (logged): the category **glyph inside the map teardrop** — needs 12 white glyph sprites in `gen-brand-assets.mjs` (fiddly lucide→PNG); the composer/card chapitas already carry the glyph, so the branding-forward feel lands without it. Follow-up for a later pass.
- **W5 Voice**: warmer es-AR on empty states/CTAs in `strings.ts` (rail empty, feed empty/end, `noComments`), no emoji. New composer/FAB/rail strings.
- **Footer**: `site-footer.tsx` is now route-aware — hidden on the app-shell routes (`/`, `/novedades`, `/p/*`) where the full-screen map + FAB would clash; legal links stay on every content page.
- **Verified**: `npm run lint` + `npm run build` clean. **Playwright smoke 2/2** (updated the composer step — single sheet, no "Confirmar ubicación" gate; comment composer still drives via the same placeholder, which focuses→expands the compact bar). Manual Playwright screenshots (real WebGL via the SwiftShader flags): desktop + mobile home (rail segmented control, warm FAB), rail switching to Nuevos, `/novedades` landing on the map, decluttered **panel with hero photo + sticky composer + collapsed "cerca"**, and the **single-sheet composer with the live chapita preview** (typed "se cortó la luz" → auto-picked **Ámbar · Luz**). Dark-mode tokens present (theme system unchanged from Sprint B).
- **Anonymity boundary: N/A, stated explicitly** — no queries/views/RPCs changed. The rail feed modes reuse the existing anonymity-safe `feed` RPC.
- Deviations/notes: (a) smart-suggest runs from the text change handlers (not an effect) to satisfy `react-hooks/set-state-in-effect`. (b) glyph-in-teardrop deferred (see W4). (c) the mobile map-collapse stayed the existing toggle (a fully draggable bottom sheet was scoped as optional polish and deferred).
- Blocked/open: none. Next: Sprint 6 — production launch (owner-gated).

## 2026-07-21 — Sprint E: Map "feel alive" pass (MILESTONE COMPLETE)
Goal: fix three owner-flagged map issues — it "feels empty", cluster (blue-dot) taps don't open the way you'd expect, and the pin color/icon aren't meaningful on the map. Extra sprint (owner request), decisions-log **P14**. Owner picked **zoom-to-fit then mini-list** for cluster taps. **Presentation-layer only — no migration/view/RPC/query touched.**
- **#3 Category glyph on every teardrop (the deferred Sprint-D W4 item, now shipped).** New `scripts/gen-brand-assets.mjs` section renders 12 white lucide glyphs (`public/map/glyph-*.png`) from lucide-react's raw `__iconNode` arrays (**no new dependency**; canonical module names — aliases only re-export `default`). Each glyph is drawn into the **same canvas geometry as the teardrop** (`PIN_VIEWBOX`, height 120), centred on the head, so the map's new `pins-glyph` symbol layer reuses the teardrop layer's exact `icon-anchor`/`icon-size` and overlays pixel-perfectly at every zoom — no `icon-offset` math (the alignment concern that got W4 deferred). Teardrops **lost their white "eye" dot** (`mapPinSvg`) so the glyph shows, matching `ChapitaPreview`. `GLYPH_IMAGE_KEYS`/`glyphImageId` in `pin-style.ts`; `addPinImages` registers glyphs too (+ re-adds on `styledata` after a theme swap). The open-pin `SelectedPin` HTML marker + `ChapitaPreview` glyph nudged to `top-[29%]` (owner: glyphs sat a couple px high).
- **#2 Clusters that open (`map-view.tsx` `handleClick` + new `components/map/cluster-popup.tsx`).** Tapping a cluster now reads its leaves (`getClusterLeaves`), computes their bbox, and **`fitBounds`** to fan them apart in one gesture (replaces the old single-step `easeTo` that often left a smaller cluster). When the leaves are effectively coincident (<~50 m span, `COINCIDENT_DEG`) it opens a **mini-list `ClusterPopup`** (color+glyph chip + title rows; each opens that thread) — no extra fetch, leaves already carry `PinFeatureProps`. `clusterList` state cleared on open/close and on map move.
- **#1 Feels empty.** Looser clustering (`clusterRadius 50→40`, `clusterMaxZoom 14→13`) so the default city zoom (14) renders individual colorful glyph teardrops instead of a few blobs. New `map.clusterCount` string. **Note:** true density also needs real content — seeding more launch pins is an **owner content task**, not code.
- **Verified**: `npm run lint` + `npm run build` clean. **Playwright smoke 2/2** (no map-interaction selectors touched; symbol-layer changes didn't break pin select/open). Real-WebGL Playwright screenshots (SwiftShader flags) **desktop + mobile, light + dark**: white category glyphs render in the teardrop heads and align (verified against a composited pin+glyph preview across 6 color/icon combos); dormant ghost pins render (grey, translucent, glyph visible); zoomed-out clusters show the step-colored count bubbles; **clicking the 25-, 8- and 19-count clusters each fanned their pins apart via `fitBounds`** and the rail count updated; default view un-clusters to individual pins. 
- **Anonymity boundary: N/A, stated explicitly** — no queries/views/RPCs changed.
- Deviations/notes: (a) the **coincident mini-list path** couldn't be reproduced against the current seed — every cluster's members are geographically spread (all hit `fitBounds`); the only truly-coincident points are the smoke test's ~19 identical-coord "Bache de prueba" pins, which always share a cluster with spread neighbours (>50 m bbox) at z≤13 and un-cluster into a single stacked teardrop above z13. The list path is verified by code review + reuses the proven `Popup` pattern; with real (distinct-coord) user pins it's a rare fallback, and the rail always lists everything regardless. (b) glyph layer is **not** in `interactiveLayerIds` — clicks/hover fall through to the teardrop feature underneath (same point), so selection is unaffected. (c) `PIN_ICON_SIZE`/`PIN_ICON_OPACITY` extracted as shared `ExpressionSpecification` consts so the two symbol layers can't drift.
- Blocked/open: none. Next: Sprint 6 — production launch (owner-gated).

## 2026-07-21 — Sprint F: Shareable pins — CTA share menu + base62 short links (MILESTONE COMPLETE)
Goal: make pins easy to post to X/Twitter — a clear share CTA and shorter links. Owner-requested distribution pass, decisions-log **P15**. Owner picked the native-sheet + "Compartir en X" + "Copiar link" shape and **base62 UUID encoding** for links (no schema change, no third-party/short-domain). **URL/presentation-layer only — no migration/view/RPC/query touched.**
- **Short links (`lib/pin-link.ts`, new).** `encodePinId`/`decodePinId`/`pinPath` map a pin's UUID to a 22-char base62 code and back — pure BigInt, bijective, no lookup table, no collisions. `/p/{code}` replaces `/p/{uuid}` everywhere links are built (`pin-card`, `comment-rows`, `admin-screen`, `nuevo` `router.push`, the panel share URL). Decode happens at the route boundary (`app/p/[id]/page.tsx` `generateMetadata` + default export → `MapView initialPinId`, and `app/p/[id]/og/route.tsx`) and in the map's shallow routing (`map-view.tsx`: `pathnamePinId` decodes, `openPin` pushes `pinPath`). **`decodePinId` returns a raw UUID untouched, so legacy `/p/{uuid}` links and any cached OG fetches still resolve** — verified. BigInt *literals* need ES2020; tsconfig is lower, so the codec uses `BigInt(0)`/`BigInt(62)` consts, not `0n`/`62n`.
- **Share CTA (`pin-panel.tsx`).** The muted engagement-bar share text becomes a bordered CTA. On `navigator.share` devices (mostly mobile) the trigger goes straight to the **OS share sheet** (controlled `DropdownMenu` `onOpenChange` short-circuits before opening); on desktop it opens a menu with **"Compartir en X"** (`twitter.com/intent/tweet`, text = pin title, `hashtags=QuePaso,Rosario`, new tab) and **"Copiar link"** (clipboard + the existing "¡Link copiado!" toast). New strings `pin.shareOnX` / `pin.copyLink`.
- **Verified**: `npm run lint` + `npm run build` clean. Codec unit-checked (100k random-UUID roundtrips lossless, uniform 22-char length, all-zero + legacy-passthrough cases). **Playwright smoke 2/2** — moderation loop now publishes → **routes to `/p/{base62}`** → panel opens (proves encode + shallow routing + deep-link) → comment → like → report → admin dismiss; smoke URL assertion updated from `[0-9a-f-]{36}` to `[0-9A-Za-z-]{22,36}`. Extra throwaway specs (run then deleted) confirmed: rail links are base62, short-link **reload re-opens the pin** (SSR decode), desktop share menu shows both items, **legacy UUID link still opens the pin**, and the **native-share branch** fires `navigator.share` with the encoded URL and does *not* open the menu.
- **Anonymity boundary: N/A, stated explicitly** — no queries/views/RPCs changed.
- Deviations/notes: (a) **AGENTS.md §9 fixed** — the quick-ref said `npx playwright test`, which pulls a *different* Playwright version and dies with "test() … not expected here"; corrected to **`npm run e2e`** (pinned local `@playwright/test`). This bit a prior session too. (b) Twitter honesty caveat (recorded in P15): link length doesn't affect the tweet — X wraps every URL to a 23-char `t.co`; base62 helps WhatsApp/IG/verbal sharing, the button + pre-filled tweet is the real lever.
- Blocked/open: none. Next: Sprint 6 — production launch (owner-gated).

## Pending owner tasks (carry forward until done)
- **Short share domain (Sprint F, optional).** A dedicated short domain (e.g. `qpso.ar`) is the only shorter-link lever we can't self-host — needs owner + NIC.ar (CUIT/CUIL), same as the main domain. Links already work at `quepaso.com.ar/p/{22-char}` without it.
- **Site URL for OG (Sprint B / deploy).** Set `NEXT_PUBLIC_SITE_URL` in Vercel to the production origin so social-share image URLs resolve absolutely (falls back to `http://localhost:3000` until then). Documented in `.env.example`.
- **Sentry DSN (Sprint 5 / deploy).** Create a free Sentry project; set `NEXT_PUBLIC_SENTRY_DSN` in Vercel. The SDK is wired and no-ops until then.
- **Contact email.** `/privacidad` + footer use the placeholder `hola@quepaso.com.ar`; point it at a real inbox (in `lib/strings.ts` `footer.contactEmail`) when the domain/email exists.
- **Google OAuth setup (Sprint 2 / deploy).** Create a Google Cloud OAuth client; add redirect URIs (`https://<project-ref>.supabase.co/auth/v1/callback` for hosted; `http://127.0.0.1:55321/auth/v1/callback` for local). Put credentials in Supabase dashboard (hosted) and/or `SUPABASE_AUTH_GOOGLE_CLIENT_ID`/`_SECRET` env + `enabled=true` in `config.toml` (local). Until then, local uses the dev email form. See deployment-guide §3.6.
- **Hosted deploy (Sprint 0 step 7), deferred 2026-07-06.** Create a Supabase project (region `sa-east-1`) and a Vercel project (root dir `web/`, env vars `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY`). Steps: `deployment-guide.md` §3–§4. App is build-clean and Vercel-ready. Not required for local Sprint 1+ work.
